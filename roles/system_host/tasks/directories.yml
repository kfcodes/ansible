---
- name: Ensure application root exists
  ansible.builtin.file:
    path: "{{ system_host_app_root }}"
    state: directory
    owner: "{{ system_host_app_root_owner }}"
    group: "{{ system_host_app_root_group }}"
    mode: "{{ system_host_app_root_mode }}"
