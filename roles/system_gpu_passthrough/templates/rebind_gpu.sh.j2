#!/usr/bin/env bash
set -euo pipefail

echo "This host is configured to keep the passthrough GPU bound to vfio-pci at boot."
echo "Rebinding to host drivers is intentionally not automated here."
echo
echo "If you really need to rebind, do it manually by binding to the correct driver:"
echo "  - amdgpu / radeon for VGA"
echo "  - snd_hda_intel for HDMI audio"
exit 0
