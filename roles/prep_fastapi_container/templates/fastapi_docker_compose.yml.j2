version: '3'

# docker-compose.yml
services:
  api:
    build: .
    container_name: "{{ item.repo_name }}"
    volumes:
      - ./app:/app
    ports: 
      - "{{ item.port }}:8000"
    restart: unless-stopped

