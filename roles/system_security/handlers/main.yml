---
- name: validate sshd config
  ansible.builtin.command: sshd -t
  changed_when: false

- name: reload sshd
  ansible.builtin.service:
    name: "{{ system_security_ssh_service_name }}"
    state: reloaded
