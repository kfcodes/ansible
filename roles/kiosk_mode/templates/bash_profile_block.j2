# Auto-start X only on {{ kiosk_autologin_tty }}.
# This avoids starting kiosk sessions over SSH or on other TTYs.
if [[ -z "$DISPLAY" ]] && [[ "$(tty)" == "/dev/{{ kiosk_autologin_tty }}" ]]; then
  exec startx
fi
