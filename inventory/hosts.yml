all:
  children:
    local_systems:
      hosts:
        dev_system: {}

    host_systems:
      children:
        frontend_reactapp
        # backend_labelapi
        # backend_palletapi

    frontend_reactapp:
      hosts:
        laptop_l: {}

    # backend_labelapi:
    #   hosts:
    #     host2: {}
    #
    # backend_palletapi:
    #   hosts:
    #     host2: {}
