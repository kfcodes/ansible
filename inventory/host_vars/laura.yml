---
connection_mode: "ip" # "ip" or "hostname"
host_ip: "192.168.1.90"
system_hostname: "host-laptop"

# Use IP now; flip connection_mode to "hostname" later to connect by name
ansible_host: "{{ host_ip if connection_mode == 'ip' else system_hostname }}"

network:
  configure: true
  interface: "enp3s0"
  type: "ethernet"
  static:
    enabled: true
    address: "{{ host_ip }}/24"
    gateway: "192.168.1.1"
    dns:
      - "1.1.1.1"
      - "1.0.0.1"
